<Page
    x:Class="Nagi.WinUI.Pages.DownloadsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Nagi.WinUI.Models"
    Background="Transparent"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24,24,24,40" Spacing="24" MaxWidth="900">

            <!-- Section 1: URL Downloader -->
            <StackPanel Spacing="8">
                <TextBlock Text="URL Download"
                           Style="{StaticResource SubtitleTextBlockStyle}" />
                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             PlaceholderText="Paste a YouTube or SoundCloud URL"
                             Text="{x:Bind ViewModel.UrlInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             VerticalAlignment="Center" />
                    <Button Grid.Column="1"
                            Content="Download"
                            Style="{StaticResource AccentButtonStyle}"
                            Command="{x:Bind ViewModel.DownloadCommand}"
                            VerticalAlignment="Center" />
                </Grid>
                <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                           Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                           Visibility="{x:Bind ViewModel.StatusMessage, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}"
                           Style="{StaticResource CaptionTextBlockStyle}" />
            </StackPanel>

            <!-- Section 2: SoundCloud Sync -->
            <StackPanel Spacing="8">
                <TextBlock Text="SoundCloud Sync"
                           Style="{StaticResource SubtitleTextBlockStyle}" />
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Content="Sync SoundCloud Likes"
                            Command="{x:Bind ViewModel.SyncSoundCloudCommand}"
                            IsEnabled="{x:Bind ViewModel.IsSyncing, Mode=OneWay, Converter={StaticResource BooleanToInverseBooleanConverter}}" />
                    <ProgressRing IsActive="{x:Bind ViewModel.IsSyncing, Mode=OneWay}"
                                  Width="20" Height="20" />
                </StackPanel>
                <TextBlock Text="Configure SoundCloud credentials in Settings"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- Section 3: Download Queue -->
            <StackPanel Spacing="8">
                <TextBlock Text="Download Queue"
                           Style="{StaticResource SubtitleTextBlockStyle}" />
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <ListView ItemsSource="{x:Bind ViewModel.Downloads}"
                              SelectionMode="None"
                              MinHeight="200"
                              MaxHeight="500">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Padding" Value="4" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:DownloadItem">
                                <Grid Padding="4,4,4,4" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="120" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Spacing="2">
                                        <TextBlock Text="{x:Bind Title, Mode=OneWay}"
                                                   Style="{StaticResource BodyTextBlockStyle}"
                                                   TextTrimming="CharacterEllipsis"
                                                   MaxLines="1" />
                                        <TextBlock Text="{x:Bind StatusMessage, Mode=OneWay}"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                   TextTrimming="CharacterEllipsis"
                                                   MaxLines="1" />
                                    </StackPanel>
                                    <Border Grid.Column="1" VerticalAlignment="Center"
                                            Background="{ThemeResource AccentFillColorDefaultBrush}"
                                            CornerRadius="4" Padding="4,2" HorizontalAlignment="Left">
                                        <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}">
                                            <Run Text="{x:Bind Source}" />
                                        </TextBlock>
                                    </Border>
                                    <ProgressBar Grid.Column="2" VerticalAlignment="Center"
                                                 Value="{x:Bind Progress, Mode=OneWay}"
                                                 Maximum="100" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
